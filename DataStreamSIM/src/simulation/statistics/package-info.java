/**
 * It implements the processing of the statistics that are gathered
 * during the simulation.
 * <p>
 * It consists of:
 * <pre>The {@link simulation.statistics.Statistics} class </pre> 
 * It collects statistics about the no. of tuples that are processed,
 * their processing times, and saves them into files.
 */
package simulation.statistics;