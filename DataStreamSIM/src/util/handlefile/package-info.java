/**
 * It contains new exception types used by the simulation.
 * <p>
 * It consists of two components:
 * <pre>The {@link util.handlefile.ReadXMLFile} class </pre> 
 * Used for parsing the configuration file <code>conf.xml</code> 
 * of the simulation.
 * 
 * <pre>The {@link util.handlefile.FileHandler} class </pre> 
 * Used for handling files.
 * 
 */
package util.handlefile;